<!DOCTYPE html>
<html class="ua_js_yes">
  <head>
    <title><%= content_for(:title) ? yield(:title) : 'Zireael' %></title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
  </head>

  <body class="page page_theme_normal i-bem" data-bem='{"page":{}}' style="zoom:1">
    <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, class: "flash flash_type_#{name}" %>
    <% end %>

    <div class="header">
      <a class="header__brand" href="/">
        Zireael
      </a>
      <div class="header__userland">
        <% if current_user %>
          Logged in as <span class="header__username"><%= current_user.username %></span>
          <%= link_to 'Logout', logout_path, method: :delete, class: 'link' %>
        <% else %>
          <%= link_to 'Sign in', login_path, class: 'link' %>
        <% end %>
      </div>
    </div>

    <div class="page__content">
      <%= yield %>
    </div>
  </body>
</html>
